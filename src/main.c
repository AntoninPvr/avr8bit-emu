#include <stdint.h>
#include <stdbool.h>
#include <stdio.h>
#include "core.h"
#include "arithm_logic.h"

void read_program(FILE file, const char *filename) {
    FILE *file = fopen(filename, "rb");
};

int main() {
    
}